import { GreedyBestFirstSearch } from './greedy-best-first-search';

describe('GreedyBestFirstSearch', () => {
  it('should create an instance', () => {
    expect(new GreedyBestFirstSearch()).toBeTruthy();
  });
});
